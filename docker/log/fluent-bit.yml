env:
  flush_interval: 1
service:
  flush: ${flush_interval}
  http_server: on
pipeline:
  inputs:
    - name: forward
      host: 0.0.0.0
      port: 24224
  outputs:
    - name: stdout
      match: '*'
